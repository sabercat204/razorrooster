"""Persistence layer — DuckDB store, schemas, staging-merge, provenance.

Populated across T-010 (schemas), T-011 (operational tables), T-012 (DuckDB
store wrapper), T-013 (migrations), T-014 (staging-merge), T-015 (provenance),
and T-016 (disk budget).
"""
