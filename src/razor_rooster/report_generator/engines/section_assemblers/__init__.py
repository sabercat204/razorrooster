"""Section assemblers (T-RG-020..T-RG-026).

Each assembler reads upstream tables and returns a structured
content dict. Renderers convert content dicts to text. The dict
shape is documented per assembler module.
"""
