"""``signal_scanner`` — live-evaluation bridge between historical patterns and current conditions (The Nose).

v1 scope (SIGNAL_SCANNER.md): per-cycle evaluation of every registered
``pattern_library`` event class against current ``data_ingest`` state,
producing per-class current-conditions probability estimates with
reasoning traces and surfacing candidate situations that diverge
materially from the base rate.
"""
